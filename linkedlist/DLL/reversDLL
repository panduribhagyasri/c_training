struct node* reverseDLL(struct node *head)
{
    if(head == NULL || head->next == NULL)
        return head;
    
    struct node *ptr = reverseDLL(head->next);
    (head->next)->next = head;
    head->prev = head->next;
    head->next = NULL;
    return ptr;
}
